ext {
  targetDir = "src/main/resources"
}

task settingEnvironment(){
  ant.replaceregexp(file: "${targetDir}/application.properties", match: "swagger.host=.+", replace: "swagger.host=${host}", byline: true)
}
